{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}

<link rel="stylesheet" href="{{ asset('/css/style_crud.css') }}"/>

<main>
    <h1>{{ categorie.nomCategorie }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom_categorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}"><button>Modifier</button></a> <br>

    {{ include('categorie/_delete_form.html.twig') }}
    
    <a href="{{ path('app_categorie_index') }}">Retour à la liste</a>
</main>
{% endblock %}
