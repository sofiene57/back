{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}

<link rel="stylesheet" href="{{ asset('/css/style_crud.css') }}"/>

<main>

    <h1>Produits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom produit</th>
                <td>{{ produit.nomProduit }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td> <img src="{{ asset('/uploads/'~produit.photo) }}" alt="" width="150"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_edit', {'id': produit.id}) }}"><button>Modifier</button></a>  <br>

    {{ include('produit/_delete_form.html.twig') }} 
    
    <a href="{{ path('app_produit_index') }}">Retour à la liste</a>

</main>

{% endblock %}
